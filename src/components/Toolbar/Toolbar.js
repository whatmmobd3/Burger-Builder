import React from 'react'
import classes from './Toolbar.css'

const App = props => (
    <header className={classes.Toolbar}>
        <div>MENU</div>
        <div>LOGO</div>
        <nav>
            ...
        </nav>
    </header>
)

export default App